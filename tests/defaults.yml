# Overwrite parameters here

parameters:
  kapitan:
    dependencies:
      - type: https
        source: https://raw.githubusercontent.com/projectsyn/component-kyverno/v1.0.0/lib/kyverno.libsonnet
        output_path: vendor/lib/kyverno.libjsonnet
