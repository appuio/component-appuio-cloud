apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  annotations: {}
  labels:
    name: appuio-ns-provisioner
  name: appuio-ns-provisioner
rules:
  - apiGroups:
      - ''
    resources:
      - namespaces
    verbs:
      - create
