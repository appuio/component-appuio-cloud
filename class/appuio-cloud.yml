parameters:
  kapitan:
    compile:
      - input_paths:
          - appuio-cloud/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - appuio-cloud/component/main.jsonnet
        input_type: jsonnet
        output_path: appuio-cloud/
